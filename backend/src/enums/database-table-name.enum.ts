const DatabaseTableName = {
  MIGRATIONS: 'migrations',
  USERS: 'users',
  MONITOR_SCRIPT_REPORTS: 'monitor_script_reports',
  MONITOR_SCRIPTS: 'monitor_scripts',
} as const;

export { DatabaseTableName };
